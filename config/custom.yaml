zotero:
  user_id: ${oc.env:ZOTERO_ID}
  api_key: ${oc.env:ZOTERO_KEY}
  include_path: null # Or set a list of glob patterns, e.g. ["2026/survey/**","2026/reading-group/**"]

email:
  sender: ${oc.env:SENDER}
  receiver: ${oc.env:RECEIVER}
  smtp_server: smtp.qq.com
  smtp_port: 465
  sender_password: ${oc.env:SENDER_PASSWORD}

llm:
  api:
    key: ${oc.env:OPENAI_API_KEY}
    base_url: ${oc.env:OPENAI_API_BASE}
  generation_kwargs:
    model: gpt-4o-mini
  language: 中文 # TL;DR 用中文输出

source:
  arxiv:
    category: ["cs.AI","cs.CV","cs.LG","cs.CL"]

executor:
  debug: ${oc.env:DEBUG,null}
  source: ['arxiv']
  max_paper_num: 7 # 每天最多 7 篇精华
  # 相关性门槛：低于该分数的论文直接丢弃（在 7 篇上限之前生效）。
  # 每次运行日志会打印 "Score distribution" 和被保留论文的分数，
  # 先跑一两天看实际分布，再把这个值调到能滤掉弱相关的位置（jina 模型一般 3~5 之间）。
  min_score: 4.3
